{{> nav/navbar-block}}
<div class="container">
    <div class="row center-align">
        <div class="col s10 userHead">
            <h3>Your Settles</h3>
        </div>
    </div>
    <!-- Div to display settles -->
    <div class="row">
        <div class="col s12 settleDiv">

            {{#each SettleIts}}
                {{> settle/settle-block}}
            {{/each}}

        </div>
    </div>
</div>  